services:
  agent:
    build: .
    container_name: rohnelt-code-hub-agent
    restart: unless-stopped
    pid: host
    environment:
      - PROJECTS_DIR=/projects
      - API_URL=${API_URL:-https://rohnelt.dev}
      - AGENT_TOKEN=${AGENT_TOKEN}
      - DEBOUNCE_MS=${DEBOUNCE_MS:-5000}
      - FULL_RESCAN_MS=${FULL_RESCAN_MS:-3600000}
      - PRESENCE_CHECK_MS=${PRESENCE_CHECK_MS:-5000}
      - PRESENCE_PROCESS=${PRESENCE_PROCESS:-antigravity}
    volumes:
      - ${PROJECTS_DIR:-/home/felipe/Documentos}:/projects:ro
      - /proc:/host/proc:ro
